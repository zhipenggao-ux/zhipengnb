混合整数线性规划（Mixed Integer Linear Programming, MILP）是一种数学优化方法，用于在满足线性约束的前提下求解线性目标函数的最优解，其核心特征在于决策变量中既包含连续实数，也包含整数或 0-1 离散值。

## 核心概念拆解

MILP 模型由决策变量、目标函数和约束条件三要素构成：

| 组成要素 | 概念说明 | 实际例子 |
| --- | --- | --- |
| **决策变量** | 需要求解的未知数，区分连续与整数变量 | $x \ge 0$（原料用量，连续）；$y \in \{0, 1\}$（是否建厂，离散） |
| **目标函数** | 衡量方案优劣的线性表达式（最大化/最小化） | $\max z = 300x_1 + 500x_2 - 1000y$ |
| **约束条件** | 变量必须满足的线性等式或不等式 | $2x_1 + 3x_2 \le 100$（资源上限） |

---

## 经典应用案例

### 案例 1：项目投资选择（0-1 决策）

**场景**：有 3 个投资项目，总预算上限为 50 万元。项目 A 需投入 20 万（收益 30 万），项目 B 需 30 万（收益 40 万），项目 C 需 40 万（收益 55 万）。如何选择能使总收益最大？

* **决策变量**：设 $x_A, x_B, x_C \in \{0, 1\}$，取 1 表示投资，取 0 表示不投。
* **目标函数**：
$$\max z = 30x_A + 40x_B + 55x_C$$


* **约束条件**：
* 预算限制：$20x_A + 30x_B + 40x_C \le 50$
* 变量约束：$x_A, x_B, x_C \in \{0, 1\}$



### 案例 2：生产计划与固定开工成本（混合变量）

**场景**：某工厂生产产品，产量为 $x$（千件）。生产每千件可获利 5 万元；但只要开启流水线，就需支付 10 万元的固定开工成本。工厂最大产能为 8 千件。

* **决策变量**：
* $x \ge 0$（产量，连续变量）
* $y \in \{0, 1\}$（开工状态：$y=1$ 表示开工，$y=0$ 表示停工）


* **目标函数**：
$$\max z = 5x - 10y$$


* **约束条件**：
* 产能与逻辑绑定约束：$x \le 8y$
* 变量取值：$x \ge 0, y \in \{0, 1\}$
*(说明：若不开工 $y=0$，约束强制 $x \le 0$ 即产量为 0；若开工 $y=1$，产量上限为 8)*



---

## Python 代码实现

利用 Python 的 `PuLP` 库解决案例 1 的求解过程如下：

```python
import pulp

# 1. 创建求解问题（最大化目标）
prob = pulp.LpProblem("Investment_Optimization", pulp.LpMaximize)

# 2. 定义 0-1 决策变量
xA = pulp.LpVariable("Project_A", cat="Binary")
xB = pulp.LpVariable("Project_B", cat="Binary")
xC = pulp.LpVariable("Project_C", cat="Binary")

# 3. 添加目标函数
prob += 30 * xA + 40 * xB + 55 * xC

# 4. 添加约束条件（预算 <= 50）
prob += 20 * xA + 30 * xB + 40 * xC <= 50

# 5. 执行求解
prob.solve()
print(f"解状态: {pulp.LpStatus[prob.status]}")
print(f"决策: 项目A={pulp.value(xA)}, 项目B={pulp.value(xB)}, 项目C={pulp.value(xC)}")
print(f"最大收益: {pulp.value(prob.objective)} 万元")

```

---

## 常用建模逻辑技巧

* **逻辑开关（Big-M 法）**：利用 $x \le M \cdot y$（$M$ 为极大的常数或实际上限）将连续变量 $x$ 的启用状态与 0-1 变量 $y$ 关联。
* **互斥选择**：若项目 A 和项目 B 只能二选一，添加约束 $x_A + x_B \le 1$。
* **先决条件**：若选择项目 B 的前提是必须先选择项目 A，添加约束 $x_B \le x_A$。
